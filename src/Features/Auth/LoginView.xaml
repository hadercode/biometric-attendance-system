<UserControl x:Class="LectorHuellas.Features.Auth.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1024">

    <Grid Background="{DynamicResource BgDarkBrush}">
        <!-- ═══ Background Decorative Elements ═══ -->
        <Path Data="M 0,0 L 400,0 L 0,400 Z" Fill="#0A6C5CE7" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Path Data="M 1000,1000 L 600,1000 L 1000,600 Z" Fill="#0A00CEC9" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>

        <Border Width="400" VerticalAlignment="Center" HorizontalAlignment="Center"
                Style="{StaticResource CardBorder}" Padding="40,48">
            <StackPanel>
                <!-- Icon & Header -->
                <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                <TextBlock Text="Acceso Administrativo" FontSize="22" FontWeight="Bold" 
                           Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Ingrese sus credenciales para continuar" FontSize="13" 
                           Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,8,0,32"/>

                <!-- Username -->
                <TextBlock Text="Usuario" FontSize="12" FontWeight="SemiBold" 
                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                <TextBox Style="{StaticResource ModernTextBox}" 
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,20"/>

                <!-- Password -->
                <TextBlock Text="Contraseña" FontSize="12" FontWeight="SemiBold" 
                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                <PasswordBox x:Name="LoginPasswordBox" Style="{StaticResource ModernPasswordBox}"
                             PasswordChanged="PasswordBox_PasswordChanged"
                             Margin="0,0,0,12"/>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                           FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,24"
                           TextWrapping="Wrap" TextAlignment="Center"/>

                <!-- Login Button -->
                <Button Style="{StaticResource PrimaryButton}" Content="Iniciar Sesión"
                        Command="{Binding LoginCommand}" Padding="0,14" Margin="0,0,0,16"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
